# Scene-probe hand-labeling corpus. The class dirs hold local copies/symlinks of
# personal images used to train the 4-class scene probe — never commit those.
# This file + README.md stay tracked; the image corpus does not.
interior/
exterior/
place/
neither/
# graphic/ is tracked (own images, committed 6f61449) EXCEPT third-party pulls:
# Crello/VistaCreate forbids redistributing the source templates, so the
# generated previews stay local. Regenerate with
# scripts/download_crello_graphics.py.
graphic/crello_*
graphic/crello_manifest.csv
